# Canonical benchmark artifacts

`harbor.mecob` is regenerated from `benchmarks/packages/harbor/root.meco` with its message manifest
by the CLI. It is a checked-in `bytecode/1` fixture for native, Deno, browser, and embedding
measurements. `workloads/` contains the six canonical scaling artifacts.
